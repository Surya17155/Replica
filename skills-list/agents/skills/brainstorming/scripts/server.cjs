This file is a Node.js server (server.cjs) for the Superpowers Brainstorm Visual Companion. It implements raw RFC 6455 WebSocket handling, HTTP request serving, file watching, session management with per-session secret key authentication, browser auto-launch, idle timeout (default 4h), and owner-PID death detection. Serves HTML content files from a watched content directory, auto-refreshes browser on new screens, and records user click/choice events to state directory. Uses crypto for security and supports platform-specific browser launchers (macOS, Windows, Linux).